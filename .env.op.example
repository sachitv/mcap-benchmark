# Example 1Password env file for `op run --env-file`
#
# Usage:
#   op signin                 # ensure you are signed in
#   op run --env-file .env.op -- poetry run download-benchmark my-bucket --prefix test/run1
#   op run --env-file .env.op -- poetry run tigris-mcap-benchmark /path/to/file.mcap my-bucket --prefix test/run1
#
# Notes:
# - Values on the right can be 1Password references (op://...) or literal values.
# - If you change the vault/item/field names, update the refs accordingly.
# - `AWS_SESSION_TOKEN` is optional for long‑lived keys; include it for STS/temporary creds.

# Required AWS credentials
AWS_ACCESS_KEY_ID=op://My Vault/AWS Keys/username
AWS_SECRET_ACCESS_KEY=op://My Vault/AWS Keys/credential
# Optional session token
# AWS_SESSION_TOKEN=op://My Vault/AWS Keys/session

# Optional: default bucket and endpoint override
# S3_BUCKET=my-bucket-name
# S3_ENDPOINT_URL=https://t3.storage.dev

